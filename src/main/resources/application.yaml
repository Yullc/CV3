spring:
  application:
    name: CV3

  datasource:
    url: jdbc:h2:mem:testdb;NON_KEYWORDS=KEY,VALUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  sql:
    init:
      mode: always

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.example.CV3.vo